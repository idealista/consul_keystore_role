---
- hosts: all
  pre_tasks:
    - name: Set Vagrant Consul interface
      set_fact:
        consul_interface: "eth1"
      when: ansible_user == "vagrant"
  roles:
    - consul

- hosts: consul01
  roles:
    - consul_keystore-role
